schema_version: 1

context:
  python_min: "3.10"
  version: "1.2.2"

package:
  name: trame-colormaps
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/trame-colormaps/trame_colormaps-${{ version }}.tar.gz
  sha256: a0c23db40681ce200791d27a376fb820b5baf886a2ced790734048a35e497c0b

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - numpy
    - trame >=3
    - trame-dataclass >=2
    - trame-vuetify >=3.2
    - vtk-base

tests:
  - python:
      imports:
        - trame_colormaps
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: Self-contained colormap module for VTK color transfer functions in Trame apps
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/Kitware/trame-colormaps

extra:
  recipe-maintainers:
    - jourdain
